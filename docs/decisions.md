# Decisiones y TODOs

Una línea por entrada, la más nueva arriba. Prefijos: `DECISION:`, `TODO:`, `CONTRATO:` (cambio a los contratos de `CLAUDE.md`).

- 2026-09-25 · DECISION: el aviso de alerta usa el texto `ALERTA:` en vez del símbolo ⚠ del spec: con la salida redirigida (Git Bash, smoke) Windows no garantiza que el símbolo se vea. `main()` fuerza UTF-8 en la salida para que las tildes se vean bien en cualquier terminal. Se agregó el producto L004 (esponja, sin movimientos) a los datos de ejemplo para que el indicador "sin ventas" muestre algo en la demo.
- 2026-09-25 · DECISION: F05.2 los indicadores de "últimos N días" cuentan hacia atrás desde el **último movimiento registrado**, no desde hoy. Con la fecha de hoy, los datos de ejemplo (hasta el 25/9) quedarían fuera de la ventana el día del oral o si la app no se usa por un tiempo, y todos los indicadores saldrían vacíos. Cobertura sin salidas en el período = NaN ("sin ventas"), no infinito.
- 2026-09-25 · DECISION: el menú despacha con una lista `OPCIONES` de tuplas (tecla, texto, función) y un único `try/except ErrorInventario` en el loop de `main()`, en vez de un `if/elif` de 15 ramas con un `except` en cada `opcion_*`. Agregar una opción es agregar una función y una línea; el manejo de errores queda en un solo lugar. Guardar funciones en una lista es lo único nuevo, y se explica en la guía.
- 2026-09-25 · DECISION: F01.2 `datos/movimientos.csv` tiene 72 filas de 30 días (27/8 al 25/9), no 40–60 de 21 como decía el spec: 30 días coinciden con la ventana por defecto de los indicadores de F05, así el consumo diario no queda subestimado. Los productos en alerta no tienen entradas en el período (por eso están en alerta). Generado una vez con un script descartable, no commiteado.
- 2026-09-25 · DECISION: **se agrega una fuente externa** (F08), revirtiendo el "sin fuente externa" del setup. API: DolarApi (`/v1/dolares/oficial`, sin clave), campo `venta`, para valuar inventario y reposición en USD. Módulo propio `fuente_externa.py` con `requests` y timeout de 5 s; si falla usa `datos/cotizacion.json` (se commitea una caché inicial para que la demo ande sin wifi). Alternativas descartadas: Fake Store API (productos genéricos que no son de un almacén) y un CSV de proveedor (no es una API).
- 2026-09-25 · TODO: confirmar con el docente el nombre del ZIP. La consigna dice `Apellido_Nombre_TP1.zip`; el repo y el ZIP se llaman `grupo-9` por pedido de Tomás.
- 2026-09-25 · DECISION: **POO aunque no esté en el programa** (clases 1–7 llegan hasta colecciones y funciones). Motivo: hay que explicar cómo funciona el sistema, y con `Producto`, `Movimiento` e `Inventario` cada concepto del problema tiene un lugar. Se compensa con `docs/guia.md`, que explica cada clase, y con una regla: una clase por concepto real, sin herencia ni dataclasses.
- 2026-09-25 · DECISION: cinco módulos en capas (`main`, `inventario`, `modelos`, `persistencia`, `analisis`) en vez de los dos que sugiere la consigna (`main.py` + `funciones.py`). La consigna permite adaptar la estructura; el criterio es que cada módulo tenga una sola responsabilidad y se pueda explicar en una frase.
- 2026-09-25 · DECISION: los modelos validan en el constructor y lanzan `ErrorInventario`; `main.py` atrapa esa excepción en cada opción del menú. Alternativa descartada: funciones `validar_*` que devuelven `str | None`, más simple pero obliga a chequear el retorno en cada llamada.
- 2026-09-25 · DECISION: alerta cuando `stock <= stock_minimo`; cantidad sugerida = `stock_minimo * 2 - stock` (stock objetivo = doble del mínimo). Regla simple de explicar en el oral; F05.2 agrega días de cobertura como indicador complementario, no reemplaza la regla.
- 2026-09-25 · DECISION: setup inicial. Menú CLI, dominio almacén/kiosco, todo en español, sin fuente externa (la consigna la pide "cuando resulte pertinente"; revertido después, ver F08). Sin hooks ni GitHub Actions. Tests mínimos con pytest, un archivo por módulo de lógica.
